enum UserRole {
    USER
    HOST
    ADMIN
}

enum Gender {
    MALE
    FEMALE
}

enum EventStatus {
    OPEN
    FULL
    CANCELLED
    COMPLETED
}

enum JoinStatus {
    PENDING
    ACCEPTED
    REJECTED
    LEFT
}

enum PaymentStatus {
    PENDING
    SUCCESS
    FAILED
    REFUNDED
}


enum HostApplicationStatus {
    PENDING
    APPROVED
    REJECTED
    CANCELLED
}
